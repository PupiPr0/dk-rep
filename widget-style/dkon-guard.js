if (window.self === window.top) {
    const currentUrl = window.location.href;
    if (currentUrl.includes('/embed_widget')) {
        const redirectUrl = currentUrl.replace('/embed_widget', '');
        window.location.href = redirectUrl;
    }
}
if (window.self === window.top) {
    const currentUrl = window.location.href;
    if (currentUrl.includes('/embed_widget')) {
        const redirectUrl = currentUrl.replace('/embed_widget', '');
        window.location.href = redirectUrl;
    }
}
